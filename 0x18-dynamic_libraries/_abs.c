#include "holberton.h"
/**
*_abs - computes the absolute value
*@value: integer
*Return: 1 if n positive
*/
int _abs(int value)
{
if (value < 0)
{
value = -value;
return (value);
}
else
{
return (value);
}
}
